import { Connection } from '@planetscale/database';
import { Logger } from '../logger';
import { MySqlDatabase } from '../mysql-core/db';
import { MySqlDialect } from '../mysql-core/dialect';
import { PlanetscaleQueryResultHKT, PlanetscaleSession } from './session';
export interface PlanetscaleSDriverOptions {
    logger?: Logger;
}
export declare class PlanetscaleDriver {
    private client;
    private dialect;
    private options;
    constructor(client: Connection, dialect: MySqlDialect, options?: PlanetscaleSDriverOptions);
    createSession(): PlanetscaleSession;
}
export interface DrizzleConfig {
    logger?: boolean | Logger;
}
export declare type PlanetScaleDatabase = MySqlDatabase<PlanetscaleQueryResultHKT, PlanetscaleSession>;
export declare function drizzle(client: Connection, config?: DrizzleConfig): PlanetScaleDatabase;
//# sourceMappingURL=driver.d.ts.map