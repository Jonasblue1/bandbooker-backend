import { Client, Pool, PoolClient, QueryResult, QueryResultRow } from '@neondatabase/serverless';
import { Logger } from '../logger';
import { PgDialect } from '../pg-core/dialect';
import { SelectFieldsOrdered } from '../pg-core/query-builders/select.types';
import { PgSession, PreparedQuery, PreparedQueryConfig, QueryResultHKT } from '../pg-core/session';
import { Query } from '../sql';
import { Assume } from '../utils';
export declare type NeonClient = Pool | PoolClient | Client;
export declare class NeonPreparedQuery<T extends PreparedQueryConfig> extends PreparedQuery<T> {
    private client;
    private params;
    private logger;
    private fields;
    private rawQuery;
    private query;
    constructor(client: NeonClient, queryString: string, params: unknown[], logger: Logger, fields: SelectFieldsOrdered | undefined, name: string | undefined);
    execute(placeholderValues?: Record<string, unknown> | undefined): Promise<T['execute']>;
    all(placeholderValues?: Record<string, unknown> | undefined): Promise<T['all']>;
    values(placeholderValues?: Record<string, unknown> | undefined): Promise<T['values']>;
}
export interface NeonSessionOptions {
    logger?: Logger;
}
export declare class NeonSession extends PgSession {
    private client;
    private logger;
    constructor(client: NeonClient, dialect: PgDialect, options?: NeonSessionOptions);
    prepareQuery<T extends PreparedQueryConfig = PreparedQueryConfig>(query: Query, fields: SelectFieldsOrdered | undefined, name: string | undefined): PreparedQuery<T>;
    query(query: string, params: unknown[]): Promise<QueryResult>;
    queryObjects<T extends QueryResultRow>(query: string, params: unknown[]): Promise<QueryResult<T>>;
}
export interface NeonQueryResultHKT extends QueryResultHKT {
    type: QueryResult<Assume<this['row'], QueryResultRow>>;
}
//# sourceMappingURL=session.d.ts.map